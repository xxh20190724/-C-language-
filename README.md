# C

## how to use git 

```
git config --global user.name  'Yourname'
git config --global user.email 'Youremail'
git clone 'http.....'
cd clonefile
git add file
git commit -m 'instruction'
git push

```
